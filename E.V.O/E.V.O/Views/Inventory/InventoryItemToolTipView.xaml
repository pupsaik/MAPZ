<UserControl x:Class="E.V.O_.Views.Inventory.InventoryItemToolTipView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:E.V.O_.Views.Inventory"
             xmlns:convs="clr-namespace:E.V.O_.UI.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <convs:SignedNumberConverter x:Key="SignedNumberConverter"/>
    </UserControl.Resources>

    <Grid Width="100" Height="50">
        <Grid.Background>
            <ImageBrush ImageSource="pack://application:,,,/Resources/Icons/BuildingActionTextBlockBackground.png"/>
        </Grid.Background>

        <ItemsControl ItemsSource="{Binding ItemEffectVMs}" Width="150" Height="50">

            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <WrapPanel>
                        <TextBlock Margin="0 0 5 0" Text="{Binding Amount, Converter={StaticResource SignedNumberConverter}}"/>
                        <Image Source="{Binding Image}" Margin="0 0 5 5" Height="15" Width="15" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding Name}"/>
                    </WrapPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

    </Grid>
</UserControl>
